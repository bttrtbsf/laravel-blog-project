

# 📅 Proje Geliştirme Günlüğü: Laravel Blog (Admin & Frontend)

**Tarih:** 4 Ocak 2026
**Konu:** CRUD İşlemlerinin Tamamlanması, Hata Ayıklama ve Frontend Entegrasyonu

---

## 1. Güncelleme (Update) İşlemi ve Parametre Hatası

### 🚨 Karşılaşılan Hata
`edit` fonksiyonunu yazarken aşağıdaki hatayı aldık:
> **Illuminate\Routing\Exceptions\UrlGenerationException**
> Missing required parameter for [Route: admin.gonderiler.update] [URI: admin/gonderiler/{gonderiler}] [Missing parameter: gonderiler]

### 🔍 Nedeni
Laravel, `Route::resource` kullanıldığında rota parametresini otomatik olarak çoğul isimlendirmişti (`{gonderiler}`). Ancak biz Controller ve View tarafında tekil değişken (`$gonderi`) kullanınca, Laravel isim uyuşmazlığı yaşadı ve parametreyi bulamadı.

### ✅ Çözüm
Controller tarafında "Route Model Binding" (otomatik bulma) yerine **Manuel ID** yöntemine geçerek sorunu çözdük.

**GonderiController.php (Son Hali):**
```php
public function edit($id) // Gonderi nesnesi yerine direkt ID aldık
{
    $gonderi = Gonderi::findOrFail($id);
    return view('admin.gonderiler.edit', compact('gonderi'));
}

public function update(Request $request, $id)
{
    // ... validasyon kodları ...
    $gonderi = Gonderi::findOrFail($id);
    $gonderi->update([...]);
    // ...
}

2. Silme (Delete) İşlemi ve "Hayalet" Hata
Silme butonu için HTML formumuzu ve Controller metodumuzu hazırladık ancak ilginç bir durumla karşılaştık: Kod hata vermiyor, "Başarılı" diyor ama veritabanından veri silinmiyordu.

🕵️‍♂️ Hata Ayıklama (Debugging) Adımları
Güvenlik: @csrf ve @method('DELETE') kullanımı öğrenildi. Bunlar olmadan işlemin 419 Page Expired hatası vereceği not edildi.

Rota Kontrolü: dd($id) ile ID'nin Controller'a ulaştığı doğrulandı.

Veritabanı Kontrolü: php artisan tinker üzerinden delete() komutu denendi ve çalıştığı görüldü.

HTML Yapısı: Formun <table> içinde yanlış yerde durup durmadığı kontrol edildi. Doğruydu (<td> içinde).

Model Ayarları: Gonderi.php modeline protected $table ve protected $primaryKey tanımları eklenerek Laravel'e tablo zorla tanıtıldı.

🛠️ Geçici Çözüm (Hard Fix)
Sorunun Model katmanında olduğu düşünülerek DB::table(...) yöntemi ile doğrudan SQL sorgusu atıldı ve silme işlemi gerçekleşti.

🎯 Asıl Neden ve Kesin Çözüm
Sorunun çok küçük bir yazım hatası olduğu tespit edildi.

Hatalı Kod: $gonderi->delete; (Parantez yok, değişken gibi çağrılmış).

Doğru Kod: $gonderi->delete(); (Parantez var, fonksiyon çağrılmış).

Parantezler eklendikten sonra Model yapısına geri dönüldü ve sistem kusursuz çalıştı.

3. Frontend (Ön Yüz) Geliştirme
Admin panelinden çıkıp, sitenin ziyaretçilere görünen yüzünü tasarladık.

🏠 Anasayfa (Home)
HomeController oluşturuldu.

Sadece "Taslak Olmayan" (where('taslak', 0)) yazılar çekildi.

welcome.blade.php, Tailwind CSS ile modern bir blog listesi haline getirildi.

Admin Linki: Sadece giriş yapmış kullanıcılara (@auth) görünen gizli bir "Admin Paneli" kısayolu eklendi.

📄 Detay Sayfası (Show)
Her yazı için front.show isminde yeni bir rota tanımlandı.

show($id) metodu ile tekil yazı verisi çekildi.

{!! nl2br(e($gonderi->icerik)) !!} kullanılarak metin formatı korundu.

4. Kod Özeti (Dosya Bazlı)
routes/web.php
PHP

// Anasayfa Rotası
Route::get('/', [HomeController::class, 'index'])->name('home');
// Detay Rotası
Route::get('/gonderi/{id}', [HomeController::class, 'show'])->name('front.show');

// Admin Rotaları
Route::prefix('admin')->name('admin.')->middleware('auth')->group(function () {
    Route::resource('gonderiler', GonderiController::class);
});
app/Models/Gonderi.php
PHP

class Gonderi extends Model
{
    protected $table = 'gonderiler';
    protected $primaryKey = 'id';
    protected $fillable = ['baslik', 'icerik', 'taslak', 'kullanici_id'];
    
    // İlişkiler tanımlı (kullanici, yorumlar, kategoriler vs.)
}
🚀 Sonuç
Şu an elimizde;

Kullanıcı girişi yapabilen,

Yazı ekleyip, düzenleyip, silebilen (CRUD),

Ziyaretçilere yazıları listeleyen ve detayını gösteren,

Veritabanı ilişkileri hazır (Model dosyasında), Tam fonksiyonel bir Blog Altyapısı bulunmaktadır.


Proje Geliştirme Günlüğü: Laravel Blog Projesi
Son Güncelleme: 21 Ocak 2026 Durum: Admin Paneli (Kullanıcı & Gönderi CRUD), Frontend Temelleri ve GitHub Yedeklemesi Tamamlandı.

🏗️ Bölüm 1: Proje Anatomisi ve MVC Yapısı
Laravel'in klasör yapısı ve projenin işleyiş mantığı kavrandı:

app/Http/Controllers: Trafik polisleri (Mantık burada kurulur).

resources/views: Görünen yüz (Blade dosyaları, HTML/CSS).

routes/web.php: Harita (URL adreslerinin nereye gideceği).

database/migrations: Veritabanı tarihçesi ve tablo yapıları.

public: Dış dünyaya açık kapı (CSS, JS, Resimler).

👤 Bölüm 2: Kullanıcı (User) Yönetimi ve Admin Paneli
Adminlerin yeni üye ekleyebilmesi, düzenleyebilmesi ve silebilmesi için bir modül geliştirdik. Bu süreçte Veritabanı vs. Laravel dil uyuşmazlıkları çözüldü.

🚨 Karşılaşılan Kritik Hatalar ve Çözümleri
1. Rota Çakışması (Matruşka Hatası) 🪆
Sorun: web.php dosyasında iç içe prefix('admin') grupları oluşturulduğu için URL /admin/admin/users haline geldi ve route('admin.users.create') bulunamadı hatası verdi.

Çözüm: İçteki gereksiz grup silinerek yapı sadeleştirildi.

2. Tailwind CSS ve Görünmez Butonlar 👻
Sorun: Buton kodları doğru olmasına rağmen ekranda sadece beyaz yazı görünüyordu.

Neden: Vite derleyicisi çalışmadığı için yeni eklenen bg-blue-500 gibi sınıflar işlenmemişti.

Çözüm: Geliştirme sürecinde npm run dev komutunun açık kalması gerektiği öğrenildi.

3. Veritabanı Dil Uyuşmazlığı (Ad vs Name) 🇹🇷🇬🇧
Sorun: Formdan veri gönderiliyor ancak veritabanı hatası (null value in column "ad") alınıyordu.

Neden: Laravel varsayılan olarak name beklerken, bizim veritabanımızda (DrawSQL şeması gereği) sütun adı ad ve soyad idi.

Çözüm:

View: Input isimleri name="ad", name="soyad" yapıldı.

Controller: $request->ad şeklinde karşılandı.

Model: $fillable dizisine ['ad', 'soyad'] eklendi.

4. Rol Kaydetme Sorunu (User vs Yazar) 🎭
Sorun: Formdan "Yazar" seçilmesine rağmen veritabanına varsayılan "User" olarak kaydoluyordu.

Teşhis: dd($request->all()) kullanılarak formdan gelen verinin role olduğu, ancak veritabanının rol sütununu beklediği tespit edildi.

Çözüm: Controller içinde manuel eşleştirme yapıldı:

PHP

'rol' => $request->role, // DB Sütunu => Form Inputu
Ek Ayar: Migration dosyasındaki enum değerleriyle formdaki value değerlerinin (yazar/author) aynı olması sağlandı.

📝 Bölüm 3: Gönderi (Post) Yönetimi
Blog yazılarının yönetimi için CRUD (Create, Read, Update, Delete) yapısı kuruldu.

🐛 Hata Ayıklama Notları
Delete İşlemi: $gonderi->delete; yazıldığı için silme işlemi çalışmıyordu. Parantez eklenerek $gonderi->delete(); yapıldı ve düzeldi.

Route Parametreleri: Route::resource otomatik olarak {gonderiler} çoğul parametresi atadı, Controller'da ID ile yakalayarak bu karmaşayı çözdük.

🎨 Bölüm 4: Frontend (Ön Yüz) Entegrasyonu
Admin paneli dışındaki ziyaretçi sayfaları tasarlandı.

HomeController: Sadece taslak olmayan yazıları çeken bir yapı kuruldu.

Welcome Blade: Anasayfada blog listesi Tailwind CSS ile tasarlandı.

Giriş Kontrolü: Sadece giriş yapmış kullanıcılara görünen (@auth) "Dashboard" linki eklendi.

☁️ Bölüm 5: Versiyon Kontrolü (Git & GitHub)
Proje güvenli bir şekilde GitHub'a yüklendi.

Güvenlik: .env dosyasının (şifrelerin olduğu yer) .gitignore sayesinde yüklenmediği teyit edildi.

Komutlar:

git init (Başlatma)

git add . (Sahneye alma)

git commit -m "..." (Kaydetme)

git push (GitHub'a gönderme)

💻 Güncel Kod Yapısı (Referans)
routes/web.php (Rota Haritası)
PHP

// Ziyaretçi Sayfaları
Route::get('/', [HomeController::class, 'index'])->name('home');
Route::get('/gonderi/{id}', [HomeController::class, 'show'])->name('front.show');

// Admin Paneli (Güvenli Alan)
Route::middleware(['auth', 'verified'])->prefix('admin')->name('admin.')->group(function () {
    // Kullanıcılar
    Route::resource('users', UserController::class);
    // Gönderiler
    Route::resource('gonderiler', GonderiController::class);
});
app/Models/User.php (Kullanıcı Modeli)
PHP

class User extends Authenticatable
{
    // Veritabanı sütunları ile Laravel uyumu
    protected $fillable = [
        'ad',
        'soyad',
        'email',
        'password',
        'rol', // 'role' değil 'rol'
    ];
}